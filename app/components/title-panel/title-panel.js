import './title-panel.scss'
import template from './title-panel.html'
import { Component } from '../component'

/**
 * Title Panel Component
 * Render and control title-toggle top-panel
 * @extends Component
 */
export class TitlePanel extends Component {
  /** LayerPanel Component Constructor
   * @param { Object } props.events.layerToggle Layer toggle event listener
   */
  constructor (placeholderId, props) {
  }
}
